<?php
/**
 * Шаблон настроек. Скопируйте этот файл в config.local.php и впишите значения:
 *
 *   cp config.example.php config.local.php
 *
 * config.local.php игнорируется git и никогда не попадёт в репозиторий —
 * токен бота даёт полный доступ к нему, храните его только на сервере.
 */

return [
    // Telegram: токен от @BotFather и ваш chat_id от @userinfobot
    'tg_token'  => '',
    'tg_chat'   => '',

    // Из России Telegram часто заблокирован. Если сервер до него дотянулся
    // только по IPv6, поставьте true.
    'tg_ipv6'   => false,

    // Дубль заявки на почту. Оставьте пустым, если не нужно.
    'mail_to'   => '',
    'mail_from' => '',

    // SMS на телефон через sms.ru: api_id из личного кабинета и номер.
    // Работает из России и не зависит от мессенджеров. Оставьте пустым, если не нужно.
    'sms_api_id' => '',
    'sms_to'     => '',

    // Антиспам: минимальная пауза между заявками с одного устройства, секунды
    'min_seconds_between' => 20,
];
